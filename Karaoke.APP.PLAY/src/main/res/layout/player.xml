<?xml version="1.0" encoding="utf-8"?><!--tools:showIn="@layout/home"-->
<kr.keumyoung.karaoke.play._PlayView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/player"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <SeekBar
        android:id="@+id/seek_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/bottom"
        android:layout_alignParentBottom="true"
        android:maxHeight="3dp"
        android:progress="0"
        android:theme="@style/AppTheme"
        android:visibility="visible" />

    <ImageView
        android:id="@+id/bottom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_gravity="bottom"
        android:src="@drawable/common_footer_bg"
        android:visibility="gone" />

    <include
        layout="@layout/layout_pitch_tempo"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="visible"/>

    <TextView
        android:id="@+id/txt_lyric_sleep_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:text="0"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:textColor="#FF0000"
        android:textSize="30sp"
        android:textStyle="bold"
        android:visibility="invisible" />

</kr.keumyoung.karaoke.play._PlayView>