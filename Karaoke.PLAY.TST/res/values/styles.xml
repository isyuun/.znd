<resources>

    <!-- the theme applied to the application or activity -->
    <style name="AppTheme.ActionBarOverlay" parent="AppTheme">

        <!-- Support library compatibility 17이상:불필요 -->
        <!-- <item name="android:windowActionBarOverlay">true</item> -->
        <item name="windowActionBarOverlay">true</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme.Light">

        <!-- Support library compatibility 17이상:불필요 -->
        <!-- <item name="android:windowActionBar">true</item> -->
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="windowActionBar">true</item>
        <item name="colorPrimary">#ffff9431</item>
        <item name="colorPrimaryDark">#ffD56600</item>
    </style>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="@style/Theme.AppCompat">

        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
        <item name="actionBarStyle">@style/Widget.Styled.ActionBar</item>
    </style>

    <style name="AppBaseTheme.Dark" parent="@style/Theme.AppCompat">
        <item name="actionBarStyle">@style/Widget.Styled.ActionBar</item>
    </style>

    <style name="AppBaseTheme.Light" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="actionBarStyle">@style/Widget.Styled.ActionBar</item>
    </style>

    <style name="Widget.Styled.ActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="icon">@drawable/ic_launcher</item>
    </style>

    <style name="ActionBar" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:displayOptions">homeAsUp</item>
    </style>

</resources>